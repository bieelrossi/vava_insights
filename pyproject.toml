[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vava-insights"
version = "0.1.0"
description = "Valorant performance analytics engine"
requires-python = ">=3.11"
dependencies = [
	"requests>=2.31",
]

[tool.setuptools.packages.find]
where = ["src"]
